<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ craft.app.request.getUrl() }}</title>
    <link rel="stylesheet" href="{{ url('/css/main.css') }}">
</head>
<body>
    <header>
        <div class="container">
            <h1><a href="{{ url() }}">My Travel Blog</a></h1>
            <nav>
                <ul>
                    <li><a href="{{ url('about') }}">About</a></li>
                    <li><a href="{{ url('catalog') }}">Catalog</a></li>
                    <li><a href="{{ url('blog') }}">Blog</a></li>
                </ul>
            </nav>
        </div>
    </header>
    
    <main>
        <div class="container">
            {% block content %}{% endblock %}
        </div>
    </main>
    
    <footer>
        <div class="container">
            <p>&copy; {{ "now"|date("Y") }} My Travel Blog. All rights reserved.</p>
        </div>
    </footer>
</body>
</html>
